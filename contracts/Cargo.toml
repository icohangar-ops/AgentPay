[workspace]
members = ["escrow", "service-registry"]
resolver = "2"

[workspace.package]
version = "0.1.0"
edition = "2021"
authors = ["icohangar-ops"]

[profile.release]
opt-level = "z"
lto = true
[patch.crates-io]
casper-contract = { path = "vendor_casper_contract" }
